#include <string.h>
/**
 * char *_strcat --concatinating two steings
 * @dest: fxn parameter
 * @src: fxn parameter
 * Return: always return 0
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (0);
}
